# To run: conda env create -f environment.yml
name: nerf
channels:
    - conda-forge
dependencies:
    - python=3.10
    - pip
    - numpy
    - matplotlib
    - imageio
    - imageio-ffmpeg
    - configargparse
    - cudatoolkit
    - tensorflow-gpu
    - ImageMagick